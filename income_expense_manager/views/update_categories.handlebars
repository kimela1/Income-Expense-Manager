
<h1>Update {{category_name}}:</h1>

<form id="update_categories" action="/update_categories/{{category_id}}" method="post" class="form-inline">
    New category name: <input type="text" name="category_name" value="{{category_name}}">
    <button onclick="edit_categories({{category_id}})">Update</button>
</form>